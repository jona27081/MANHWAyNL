build:
 jona27081:  # cambiar por su namespace de okteto
   image: okteto.dev/jona27081:latest  # cambiar por su namespace de okteto

deploy:
 - okteto build # build the image with the latest code
 - kubectl apply -f manifests # create all the resources in kubernetes
dev:
 jona27081:
   command: bash
   sync:
   - .:/MANHWAyNL   # cambiar por su proyecto